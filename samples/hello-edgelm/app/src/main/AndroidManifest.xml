<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Note what's NOT here: no <queries> and no <uses-permission>. The EdgeLM SDK
      contributes both (ai.edgelm.permission.USE_RUNTIME + the runtime <package>
      visibility query) through manifest merge. A consumer app declares nothing
      special to talk to the shared runtime.
    -->

    <application
        android:allowBackup="true"
        android:label="Hello EdgeLM"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.DarkActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
