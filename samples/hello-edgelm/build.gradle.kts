// Root build file for the standalone sample. Plugin versions match what the
// EdgeLM SDK is built with (AGP 8.6.1 / Kotlin 1.9.24) to avoid metadata warnings.
plugins {
    id("com.android.application") version "8.6.1" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
