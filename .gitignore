.gradle/
build/
local.properties
*.iml
.idea/
.cxx/
.DS_Store
# llama.cpp is vendored as a submodule under runtime-service/src/main/cpp/llama.cpp
# model weights are NOT committed — push them with adb (see docs/PHASE-0-BUILD-PLAN.md)
*.gguf

# Release signing — never commit the keystore or its passwords
keystore.properties
*.jks
*.keystore

# Raw device screenshots (source for the marketing shots; may contain personal info)
docs/play/screenshots/Screenshot_*.png

# Build/run logs and scratch APK output (not source)
log.txt
*.log
tmp_apk/
